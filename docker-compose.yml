version: '3.8'

services:
  beancount-web:
    image: pionnerwang/beancount-web:latest
    ports:
      - "8000:8000"
    volumes:
      - beancount-data:/app/data
    environment:
      - USERNAME=admin
      - PASSWORD=admin123
    restart: unless-stopped