# 单用户登录配置示例
# 复制此文件为 .env 并修改以下配置

# 应用密钥 (请修改为随机字符串)
SECRET_KEY=your-secret-key-change-this-in-production

# Token有效期 (分钟，默认7天)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# 登录用户名
USERNAME=admin

# 登录密码
PASSWORD=admin123

# 数据目录路径
DATA_DIR=../data

# 同步相关配置
# 延迟同步等待时间（秒），用于避免频繁同步，默认30秒
SYNC_DELAY_SECONDS=30

# 周期记账执行后的延迟同步时间（秒），默认60秒
RECURRING_SYNC_DELAY_SECONDS=60
